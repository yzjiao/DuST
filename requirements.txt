# Core dependencies
vllm>=0.8.0
torch>=2.4.0
transformers>=4.45.0
datasets>=2.20.0
pyarrow>=14.0.0
pyyaml>=6.0
jinja2>=3.1.0
numpy>=1.24.0
aiohttp>=3.9.0
huggingface_hub>=0.23.0

# Optional: S3 support
boto3>=1.34.0

# Training (verl)
# Install separately: cd third_party/verl && pip install -e .

# Eval (evalchemy)
# Install separately: cd third_party/evalchemy && pip install -e .
